#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-mincer/bin/grunt-mincer" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-mincer/bin/grunt-mincer" "$@"
  ret=$?
fi
exit $ret
